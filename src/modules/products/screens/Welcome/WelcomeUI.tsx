import { StyleSheet, View } from 'react-native';
import React from 'react';
import { CustomText } from '@components';

const WelcomeUI = () => {
  return (
    <View>
      <CustomText>WelcomeUI</CustomText>
    </View>
  );
};

export default WelcomeUI;

const styles = StyleSheet.create({});
